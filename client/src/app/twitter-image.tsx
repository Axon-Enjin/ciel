/**
 * Twitter card image — reuses the Open Graph generator so the summary_large_image
 * card matches the OG preview exactly (same twilight canvas, tagline, and
 * Dawn_Gold horizon line).
 */
export { default, alt, size, contentType } from "./opengraph-image";
